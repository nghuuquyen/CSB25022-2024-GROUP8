document.querySelectorAll('.custom-select').forEach(function(select) {
    select.addEventListener('change', function() {
      console.log('Selected value:', this.value);
    });
  });
document.querySelectorAll('.custom-rank').forEach(function(select) {
    select.addEventListener('change', function() {
      console.log('Selected value:', this.value);
    });
  });
document.querySelectorAll('.custom-characters').forEach(function(select) {
    select.addEventListener('change', function() {
      console.log('Selected value:', this.value);
    });
  });
document.querySelectorAll('.custom-skins').forEach(function(select) {
    select.addEventListener('change', function() {
      console.log('Selected value:', this.value);
    });
  });
document.querySelectorAll('.custom-sorted').forEach(function(select) {
    select.addEventListener('change', function() {
      console.log('Selected value:', this.value);
    });
  });